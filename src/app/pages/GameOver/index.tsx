import * as React from 'react';
import styled from 'styled-components/macro';

export type Props = {};

export const GameOver = (props: Props) => <Wrapper>Game over</Wrapper>;

const Wrapper = styled.div``;
